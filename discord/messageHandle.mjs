import { Client, Events, GatewayIntentBits } from "discord.js";
import { config } from "../config.mjs";
import dbConnection from "../dbconnection.mjs";

export async function manageMessage(client, message) {
    
}